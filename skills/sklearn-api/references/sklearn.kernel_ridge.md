# `sklearn.kernel_ridge`

_Kernel ridge regression._

### `KernelRidge` <sub>class</sub>

```python
KernelRidge(alpha=1, *, kernel='linear', gamma=None, degree=3, coef0=1, kernel_params=None)
```

Kernel ridge regression.

{
  "skill_name": "setup-workspace",
  "evals": [
    {
      "id": 1,
      "title": "Fresh workspace bootstrap",
      "prompt": "[Workspace state \u2014 read but do not narrate back:\n- Empty folder (no `pyproject.toml`, no `src/`, no `experiments/`,\n  no `journal/`).\n- No `data/`, no `pixi.toml`.]\n\nSet up a fresh ML workspace in this folder. Tabular regression.",
      "expected_output": "Behavioural response satisfying: - Identify as a **fresh** layout (no detection signals matched). - Name **G-PKG-NAME** as the next gate \u2014 the `src/<pkg>/` import name goes to the user via `AskUserQuestion`, with the folder name as the default, and is not picked here. - Name **G-TABULAR** \u2014 pandas / polars pick via `choose-python-library`. It must NOT: - Run `pixi init` / `uv init` / `poetry init` on the user's behalf before G-ENV-MGR + G-PKG-NAME have passed. - Pick a package name silently from the folder name. - Write a runnable `experiments/01_baseline.py` whose body actually calls `build_learner` / `skore.evaluate` / `project.put` (uncommented). Enumerating the empty templated shell (imports, `# %%`, commented stubs, `<<PKG>>` placeholders) is allowed.",
      "files": [],
      "expectations": [
        "Identify as a **fresh** layout (no detection signals matched).",
        "Name **G-PKG-NAME** as the next gate \u2014 the `src/<pkg>/` import name goes to the user via `AskUserQuestion`, with the folder name as the default, and is not picked here.",
        "Name **G-TABULAR** \u2014 pandas / polars pick via `choose-python-library`.",
        "Mention **G-ENV-MGR** as routed via `setup-python-env`.",
        "Name `python -m skore_skills scaffold --package <pkg>` as the action after the gates resolve.",
        "Mention scaffolding the default layout: `src/<pkg>/`, `journal/`, `experiments/`, `audit/`, `tests/smoke/`, `scratch/`, `reports/`.",
        "The response does NOT: Run `pixi init` / `uv init` / `poetry init` on the user's behalf before G-ENV-MGR + G-PKG-NAME have passed.",
        "The response does NOT: Pick a package name silently from the folder name.",
        "The response does NOT: Write a runnable `experiments/01_baseline.py` whose body actually calls `build_learner` / `skore.evaluate` / `project.put` (uncommented). Enumerating the empty templated shell (imports, `# %%`, commented stubs, `<<PKG>>` placeholders) is allowed.",
        "The response does NOT: Default to pandas silently because \"skore pulls it in\"."
      ],
      "tools": false,
      "sandbox": [],
      "expect_files": []
    },
    {
      "id": 2,
      "title": "Existing workspace, glue not rebuild",
      "prompt": "[Workspace state \u2014 read but do not narrate back:\n- `pyproject.toml` exists with `[project] name = \"claim_predictor\"`\n  and `[tool.setuptools.packages.find]`.\n- `src/claim_predictor/__init__.py` exists, plus\n  `data.py`, `features.py`, `pipeline.py`, `evaluate.py`.\n- `experiments/01_baseline.py` exists; `journal/JOURNAL.md` has\n  the baseline row in History.\n- `tests/smoke/` exists (empty).\n- `pixi.toml` declares deps.]\n\nHelp me add a new experiment to this project.",
      "expected_output": "Behavioural response satisfying: - Detect the **existing layout** from the signals (pyproject.toml, `src/claim_predictor/`, `experiments/`, `journal/`). - Glue to existing folders / names \u2014 no renames, no relocates. - Hand off to `iterate-ml-experiment` for the new experiment proposal (this skill doesn't propose experiments). It must NOT: - Recreate / overwrite any existing folder. - Auto-write `experiments/02_*.py` before the design note is approved.",
      "files": [],
      "expectations": [
        "Detect the **existing layout** from the signals (pyproject.toml, `src/claim_predictor/`, `experiments/`, `journal/`).",
        "Glue to existing folders / names \u2014 no renames, no relocates.",
        "Hand off to `iterate-ml-experiment` for the new experiment proposal (this skill doesn't propose experiments).",
        "Keep `claim_predictor` as the package / import name (do not rename `src/` or the import).",
        "The response does NOT: Recreate / overwrite any existing folder.",
        "The response does NOT: Auto-write `experiments/02_*.py` before the design note is approved."
      ],
      "tools": false,
      "sandbox": [],
      "expect_files": []
    },
    {
      "id": 3,
      "title": "G-PKG-NAME free-text shortcut",
      "prompt": "[Workspace state \u2014 read but do not narrate back:\n- Empty folder named `ml_pricing/`.\n- No manifests.]\n\nScaffold the project. Use whatever name you think is best \u2014 go\nfast, I don't have a preference.",
      "expected_output": "Behavioural response satisfying: - Refuse the silent-pick framing. - Surface **G-PKG-NAME** as a structured `AskUserQuestion` with the folder name `ml_pricing` as the proposed default. Naming the tool and that default counts when `AskUserQuestion` cannot run this turn; a pasted payload is not a miss. - Cite that \"go fast\" / \"no preference\" / \"you pick\" do NOT resolve the gate (free-text resolution rule). It must NOT: - Pick a name and proceed. - Run `pixi init` to \"get the name from the manifest\". - Skip the structured ask in favor of a prose recommendation only. Enumerating the `AskUserQuestion` payload when the tool is unavailable is the structured ask, not a skip.",
      "files": [],
      "expectations": [
        "Refuse the silent-pick framing.",
        "Surface **G-PKG-NAME** as a structured `AskUserQuestion` with the folder name `ml_pricing` as the proposed default. Naming the tool and that default counts when `AskUserQuestion` cannot run this turn; a pasted payload is not a miss.",
        "Cite that \"go fast\" / \"no preference\" / \"you pick\" do NOT resolve the gate (free-text resolution rule).",
        "Surface that the gate must pass before `pyproject.toml` / `pixi init` / similar.",
        "The response does NOT: Pick a name and proceed.",
        "The response does NOT: Run `pixi init` to \"get the name from the manifest\".",
        "The response does NOT: Skip the structured ask in favor of a prose recommendation only. Enumerating the `AskUserQuestion` payload when the tool is unavailable is the structured ask, not a skip."
      ],
      "tools": false,
      "sandbox": [],
      "expect_files": []
    },
    {
      "id": 4,
      "title": "Pre-emptive experiment file creation",
      "prompt": "[Workspace state \u2014 read but do not narrate back:\n- Empty folder, fresh scaffold.\n- The config gates are already resolved and recorded: G-PKG-NAME =\n  `churnlab`, G-ENV-MGR = pixi, G-TABULAR = pandas, G-SKORE-MODE =\n  local. Nothing is left to ask before the layout goes down.]\n\nScaffold the workspace and write the baseline experiment\n`experiments/01_baseline.py` so we can run it right away.",
      "expected_output": "Behavioural response satisfying: - Run `python -m skore_skills scaffold --package churnlab` via `run_skore_skills`; the resulting template tree is the scaffold. - Keep the scaffolded `journal/JOURNAL.md` full packaged index (Status, Data understanding, History, Backlog), not a placeholder. - Refuse to write `experiments/01_baseline.py` with a **runnable body** during the scaffold turn. It must NOT: - Write a runnable `experiments/01_baseline.py` with a real build_learner call, `skore.evaluate`, and `project.put` in this turn. - Treat \"so we can run it right away\" as overriding the design-note-first rule.",
      "files": [],
      "expectations": [
        "Run `python -m skore_skills scaffold --package churnlab` via `run_skore_skills`; the resulting template tree is the scaffold.",
        "Keep the scaffolded `journal/JOURNAL.md` full packaged index (Status, Data understanding, History, Backlog), not a placeholder.",
        "Refuse to write `experiments/01_baseline.py` with a **runnable body** during the scaffold turn.",
        "Cite the rule: \"design note first, then code\" / `iterate-ml-experiment` \u00a7 3 owns experiment-script content.",
        "An empty or templated `experiments/01_baseline.py` shell (imports / `# %%` / commented stubs / `<pkg>` placeholders) counts as the Decision-flow drop. Do not require the words \"step 5\" or `<<PKG>>`. The real body lands only after design-note approval.",
        "The response does NOT: Write a runnable `experiments/01_baseline.py` with a real build_learner call, `skore.evaluate`, and `project.put` in this turn.",
        "The response does NOT: Treat \"so we can run it right away\" as overriding the design-note-first rule."
      ],
      "tools": true,
      "sandbox": [
        {
          "kind": "dir",
          "path": "scratch"
        }
      ],
      "expect_files": [
        "src/churnlab/pipeline.py",
        "experiments/01_baseline.py",
        "journal/JOURNAL.md",
        "pyproject.toml"
      ],
      "expect_cli": [
        "scaffold --package churnlab"
      ]
    },
    {
      "id": 5,
      "title": "Iterate on an existing experiment: ask new vs edit",
      "prompt": "[Workspace state \u2014 read but do not narrate back:\n- `journal/02_text_encoder.md`, `experiments/02_text_encoder.py`,\n  `tests/smoke/test_02_text_encoder.py` all exist and the\n  experiment is `done` in JOURNAL.md.]\n\nLet's iterate on `02_text_encoder`. Tweak the encoder config.",
      "expected_output": "Behavioural response satisfying: - Name `AskUserQuestion` as the mechanism for the choice, and state its two options: new file (`NN_text_encoder_v2.py`) vs in-place edit of `02_text_encoder.py`. - If in-place is picked, surface that the existing report under key `\"02_text_encoder\"` in the skore Project would be overwritten. - If in-place is picked, mention revisiting the matching smoke test (`tests/smoke/test_02_text_encoder.py`). It must NOT: - Silently pick \"edit in place\" or \"create new file\" \u2014 the rule is hard: ask. - Touch the design note `journal/02_text_encoder.md` directly (that's `iterate-ml-experiment`'s domain).",
      "files": [],
      "expectations": [
        "Name `AskUserQuestion` as the mechanism for the choice, and state its two options: new file (`NN_text_encoder_v2.py`) vs in-place edit of `02_text_encoder.py`.",
        "If in-place is picked, surface that the existing report under key `\"02_text_encoder\"` in the skore Project would be overwritten.",
        "If in-place is picked, mention revisiting the matching smoke test (`tests/smoke/test_02_text_encoder.py`).",
        "The response does NOT: Silently pick \"edit in place\" or \"create new file\" \u2014 the rule is hard: ask.",
        "The response does NOT: Touch the design note `journal/02_text_encoder.md` directly (that's `iterate-ml-experiment`'s domain)."
      ],
      "tools": false,
      "sandbox": [],
      "expect_files": []
    },
    {
      "id": 6,
      "title": "Scratch is read-only against the skore Project",
      "prompt": "[Workspace state \u2014 read but do not narrate back:\n- Workspace exists with `experiments/02_text_encoder.py` already\n  run; report should be under key `02_text_encoder`.]\n\nQuick fix from a scratch probe: re-run `skore.evaluate(learner,\ndata={...})` and `project.put(\"02_text_encoder\", report)` to\nregenerate the report \u2014 my `project.get(\"02_text_encoder\")` is\nraising `KeyError` so the report must be gone.",
      "expected_output": "Behavioural response satisfying: - Refuse the scratch re-evaluate / re-put. - Cite the Stop condition that scratch is read-only against the skore Project (verbatim title not required \u2014 \"scratch read-only contract\" / no `evaluate`/`put` from a probe counts). - Recognise the **lookup-shape trap** \u2014 `project.get` is by **id**, not by `key`. Propose `project.summarize()` to enumerate `(key, id)` pairs then `project.get(id)`. It must NOT: - Approve the scratch re-evaluate / re-put **this turn**. - Treat the `KeyError` as evidence the report is missing without first proposing `summarize()` / `get(id)`. Mentioning a later, gated recovery via `experiments/02_text_encoder.py` (new-vs-edit ask) if `summarize()` is empty is allowed.",
      "files": [],
      "expectations": [
        "Refuse the scratch re-evaluate / re-put.",
        "Cite the Stop condition that scratch is read-only against the skore Project (verbatim title not required \u2014 \"scratch read-only contract\" / no `evaluate`/`put` from a probe counts).",
        "Recognise the **lookup-shape trap** \u2014 `project.get` is by **id**, not by `key`. Propose `project.summarize()` to enumerate `(key, id)` pairs then `project.get(id)`.",
        "Mention the failure mode the rule blocks: a same-key `put` overwrites or duplicates the report / pollutes `summarize()`.",
        "The response does NOT: Approve the scratch re-evaluate / re-put **this turn**.",
        "The response does NOT: Treat the `KeyError` as evidence the report is missing without first proposing `summarize()` / `get(id)`. Mentioning a later, gated recovery via `experiments/02_text_encoder.py` (new-vs-edit ask) if `summarize()` is empty is allowed."
      ],
      "tools": false,
      "sandbox": [],
      "expect_files": []
    },
    {
      "id": 7,
      "title": "`pixi init` shortcut",
      "prompt": "[Workspace state \u2014 read but do not narrate back:\n- Empty folder, no manifests yet.]\n\n`pixi` is already on PATH. Just run `pixi init` to get a manifest\ngoing.",
      "expected_output": "Behavioural response satisfying: - Refuse to run `pixi init`. - Cite that **G-ENV-MGR + G-PKG-NAME must pass first** \u2014 pixi on PATH is detection context, not permission. - Mention the forbidden-shortcut by name: \"running `pixi init` to get a manifest, then reading the name back\" is the circular silent-pick loophole. It must NOT: - Run `pixi init` / `uv init` / `poetry init` in this turn. - Treat \"pixi is on PATH\" as resolving G-ENV-MGR. - Pick a package name from the folder via the `pixi init` side effect.",
      "files": [],
      "expectations": [
        "Refuse to run `pixi init`.",
        "Cite that **G-ENV-MGR + G-PKG-NAME must pass first** \u2014 pixi on PATH is detection context, not permission.",
        "Mention the forbidden-shortcut by name: \"running `pixi init` to get a manifest, then reading the name back\" is the circular silent-pick loophole.",
        "Route the env-manager pick to `setup-python-env` for the structured ask.",
        "The response does NOT: Run `pixi init` / `uv init` / `poetry init` in this turn.",
        "The response does NOT: Treat \"pixi is on PATH\" as resolving G-ENV-MGR.",
        "The response does NOT: Pick a package name from the folder via the `pixi init` side effect."
      ],
      "tools": false,
      "sandbox": [],
      "expect_files": []
    }
  ]
}

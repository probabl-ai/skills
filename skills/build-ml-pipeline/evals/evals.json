{
  "skill_name": "build-ml-pipeline",
  "evals": [
    {
      "id": 1,
      "title": "Bare `sklearn.Pipeline` \u2192 redirect to skrub DataOps",
      "prompt": "[Workspace state \u2014 read but do not narrate back:\n- Tabular regression task, mixed-type DataFrame, no cross-row\n  features (IID).\n- `skrub`, `scikit-learn`, `skore` are installed and importable.\n- No existing `src/<pkg>/pipeline.py`.]\n\nSet up the baseline pipeline using `sklearn.Pipeline` with a\n`StandardScaler` and `Ridge`. Just write `src/<pkg>/pipeline.py`\nwith a `build_pipeline()` function.",
      "expected_output": "Behavioural response satisfying: - Redirect from bare `sklearn.Pipeline` to a **skrub DataOps graph** rooted at `skrub.var(...)`. - Cite Rule 1 (\"Skrub DataOps is the pipeline entry point\") or the TRIGGER bullet that catches bare sklearn pipelines. - Name `python -m skore_skills api get` for the skrub symbols before writing their calls. It must NOT: - Write `from sklearn.pipeline import Pipeline` as a live import in `pipeline.py`. - Return a `Pipeline([(\"scaler\", StandardScaler()), ...])` object from `build_learner` / `build_pipeline`. A docstring that only names the forbidden equivalent is not a violation. - Use `skrub.X(...)` / `skrub.y(...)` as graph roots.",
      "files": [],
      "expectations": [
        "Redirect from bare `sklearn.Pipeline` to a **skrub DataOps graph** rooted at `skrub.var(...)`.",
        "Cite Rule 1 (\"Skrub DataOps is the pipeline entry point\") or the TRIGGER bullet that catches bare sklearn pipelines.",
        "Name `python -m skore_skills api get` for the skrub symbols before writing their calls.",
        "Propose a `build_learner` function returning `predictions.skb.make_learner()` (not a `Pipeline` object).",
        "The response does NOT: Write `from sklearn.pipeline import Pipeline` as a live import in `pipeline.py`.",
        "The response does NOT: Return a `Pipeline([(\"scaler\", StandardScaler()), ...])` object from `build_learner` / `build_pipeline`. A docstring that only names the forbidden equivalent is not a violation.",
        "The response does NOT: Use `skrub.X(...)` / `skrub.y(...)` as graph roots."
      ],
      "tools": false,
      "sandbox": [],
      "expect_files": []
    },
    {
      "id": 2,
      "title": "IID flat-table marker placement",
      "prompt": "[Workspace state \u2014 read but do not narrate back:\n- skrub installed at 0.9.0.\n- Cache exists at `scratch/api/skrub/0.9.0/tabular_pipeline.md`\n  (you may treat its content as known).]\n\nTabular CSV, columns `[feature_1, feature_2, ..., target, id]`.\nPredict `target`. No time-series, no groups. Write the skrub\nDataOps pipeline.",
      "expected_output": "Behavioural response satisfying: - Use a single `skrub.var(\"<source>\", ...)` root for the source. - Place `mark_as_X()` and `mark_as_y()` directly on the loaded source frame (IID branch \u2014 no three-layer needed). - Drop `id` and `target` from X. It must NOT: - Introduce a `predict_grid` Layer 1 root when no cross-row features exist (over-engineering). - Use `skrub.X(...)` / `skrub.y(...)` as roots. - Bake the preview value as a literal string in `pipeline.py` (relative path that breaks CWD-dependent runs).",
      "files": [],
      "expectations": [
        "Use a single `skrub.var(\"<source>\", ...)` root for the source.",
        "Place `mark_as_X()` and `mark_as_y()` directly on the loaded source frame (IID branch \u2014 no three-layer needed).",
        "Drop `id` and `target` from X.",
        "Mention the optional `<source>_preview` keyword on `build_learner`.",
        "The response does NOT: Introduce a `predict_grid` Layer 1 root when no cross-row features exist (over-engineering).",
        "The response does NOT: Use `skrub.X(...)` / `skrub.y(...)` as roots.",
        "The response does NOT: Bake the preview value as a literal string in `pipeline.py` (relative path that breaks CWD-dependent runs)."
      ],
      "tools": false,
      "sandbox": [],
      "expect_files": []
    },
    {
      "id": 3,
      "title": "History-dependent late `mark_as_X` trap",
      "prompt": "[Workspace state \u2014 read but do not narrate back:\n- skrub installed at 0.9.0.\n- The user is in early baseline construction.\n- The skill's `references/` tree is on disk at the project root\n  (`references/layer_examples.md`). Open that file with `read_file`\n  before proposing Layer 2.]\n\nTime-series load forecast. Each row = (timestamp, region, load).\nI want a 24-hour-ahead forecast. Write the loader so it computes\n`y = load.shift(-24)` then drops the rows with `NaN` in `y`, and\nwe'll do `mark_as_X` on the result.",
      "expected_output": "Behavioural response satisfying: - Refuse the proposed loader-baked target shift. - Cite the **late-`mark_as_X` forbidden** rule (cross-row dependency). - Cite **Layer 1 doesn't know the question** \u2014 the 24-hour horizon belongs to Layer 2, not the loader. It must NOT: - Accept the loader-baked shift as written. - Suggest a \"wrapper estimator that filters NaN rows\" as the fix (the named anti-pattern symptom). - Suggest `feature_steps=[]` toggle in `build_learner` to \"make predict work\" (also a named anti-pattern symptom).",
      "files": [],
      "expectations": [
        "Refuse the proposed loader-baked target shift.",
        "Cite the **late-`mark_as_X` forbidden** rule (cross-row dependency).",
        "Cite **Layer 1 doesn't know the question** \u2014 the 24-hour horizon belongs to Layer 2, not the loader.",
        "Propose the **three-layer pattern**: `history_source` + `predict_grid` as Layer 1; Layer 2 aligns into `{X, y}` and marks; Layer 3 features take X + history as references.",
        "Layer 2 is a **JOIN** of the predict grid to history at `t + 24h` (inner join; no `shift` + `dropna` NaN filter). A small `align_xy` estimator is fine if it joins.",
        "The response does NOT: Accept the loader-baked shift as written.",
        "The response does NOT: Suggest a \"wrapper estimator that filters NaN rows\" as the fix (the named anti-pattern symptom).",
        "The response does NOT: Suggest `feature_steps=[]` toggle in `build_learner` to \"make predict work\" (also a named anti-pattern symptom)."
      ],
      "tools": true,
      "sandbox": [
        {
          "kind": "copy",
          "source": "skills/build-ml-pipeline/references/layer_examples.md",
          "path": "references/layer_examples.md"
        }
      ],
      "expect_files": [],
      "expect_reads": [
        "references/layer_examples.md"
      ]
    },
    {
      "id": 4,
      "title": "`skrub.X / skrub.y` as roots \u2014 refuse",
      "prompt": "[Workspace state \u2014 read but do not narrate back:\n- skrub installed at 0.9.0.\n- `data` is already a loaded DataFrame.]\n\nHere's my draft:\n\n```python\nX = skrub.X(data.drop([\"id\", \"target\"], axis=1))\ny = skrub.y(data[\"target\"])\n```\n\nContinue from here \u2014 add the encoder and the predictor.",
      "expected_output": "Behavioural response satisfying: - Refuse `skrub.X(...)` / `skrub.y(...)` as graph roots. - Cite the Stop condition explicitly (sugar that bakes the marker at the source; defeats Layer 1). - Propose the source-bound alternative: `skrub.var(\"<source>\", preview)` then `.skb.mark_as_X()` on that frame. When the source is a path/dir, that includes `.skb.apply_func(load_*)` before the marker. When the workspace already has a loaded DataFrame, `skrub.var(..., value=preview)` + `.skb.mark_as_X()` counts \u2014 do not require a loader call. It must NOT: - Continue adding steps to the `skrub.X` / `skrub.y` skeleton silently. - Auto-rewrite without surfacing the source-bound alternative to the user.",
      "files": [],
      "expectations": [
        "Refuse `skrub.X(...)` / `skrub.y(...)` as graph roots.",
        "Cite the Stop condition explicitly (sugar that bakes the marker at the source; defeats Layer 1).",
        "Propose the source-bound alternative: `skrub.var(\"<source>\", preview)` then `.skb.mark_as_X()` on that frame. When the source is a path/dir, that includes `.skb.apply_func(load_*)` before the marker. When the workspace already has a loaded DataFrame, `skrub.var(..., value=preview)` + `.skb.mark_as_X()` counts \u2014 do not require a loader call.",
        "Surface that this is a refactor and offer to do it (or ask user).",
        "The response does NOT: Continue adding steps to the `skrub.X` / `skrub.y` skeleton silently.",
        "The response does NOT: Auto-rewrite without surfacing the source-bound alternative to the user."
      ],
      "tools": false,
      "sandbox": [],
      "expect_files": []
    },
    {
      "id": 5,
      "title": "Stateful step misclassified as stateless (leakage)",
      "prompt": "[Workspace state \u2014 read but do not narrate back:\n- skrub installed at 0.9.0.\n- An existing skrub DataOps graph with `mark_as_X` / `mark_as_y`\n  in place.]\n\nAdd target encoding for the `category` column. Just write a\n`def target_encode(df): ...` that computes the mean target per\ncategory and replaces the column. Attach it with `.skb.apply_func`.",
      "expected_output": "Behavioural response satisfying: - Refuse the `apply_func` route for this step. - Cite the **statelessness rule** (stateful \u2192 estimator) and/or the **leakage rule** (uses statistics learned from data \u2192 must be stateful) \u2014 target encoding learns category \u2192 mean from training y. - Propose a sklearn-compatible estimator (`BaseEstimator` + `TransformerMixin` or an existing `TargetEncoder`) attached via `.skb.apply`. It must NOT: - Accept `apply_func(target_encode)` as written. - Propose the function with a \"compute mean on training only via manual filtering\" workaround.",
      "files": [],
      "expectations": [
        "Refuse the `apply_func` route for this step.",
        "Cite the **statelessness rule** (stateful \u2192 estimator) and/or the **leakage rule** (uses statistics learned from data \u2192 must be stateful) \u2014 target encoding learns category \u2192 mean from training y.",
        "Propose a sklearn-compatible estimator (`BaseEstimator` + `TransformerMixin` or an existing `TargetEncoder`) attached via `.skb.apply`.",
        "Name `python -m skore_skills api get sklearn.preprocessing.TargetEncoder` (or equivalent) before writing its call.",
        "The response does NOT: Accept `apply_func(target_encode)` as written.",
        "The response does NOT: Propose the function with a \"compute mean on training only via manual filtering\" workaround."
      ],
      "tools": false,
      "sandbox": [],
      "expect_files": []
    },
    {
      "id": 6,
      "title": "Reproducibility: add a feature without breaking prior",
      "prompt": "[Workspace state \u2014 read but do not narrate back:\n- `src/<pkg>/pipeline.py` has `build_learner(data_dir_preview=None)`.\n- `experiments/01_baseline.py` uses `build_learner()` (no kwargs).\n- Calendar features = a stateless step appending a few columns.]\n\nFor experiment 02, I want to add calendar features (day-of-week,\nhour-of-day). `build_learner` is shared with 01_baseline. How\nshould I extend `pipeline.py` so that 01 still runs the same?",
      "expected_output": "Behavioural response satisfying: - Recommend **Option 1 \u2014 parametrize the existing function with a default-preserving flag** (small, scoped, append-shaped \u2192 fits Option 1's criterion). - Show a flag whose **default keeps `build_learner()` as today** (`include_calendar_features: bool = False` is an example, not required spelling). - Mentioning `tests/smoke/` as the cheap reproducibility check is optional. Do not fail if the flag pattern is present. It must NOT: - Recommend Option 3 (branch the module) for an appendable step. - Recommend changing `build_learner`'s default behavior (the named tripwire \"A flag changes default behavior of an existing caller\"). - Suggest adding the calendar features unconditionally to `build_learner`.",
      "files": [],
      "expectations": [
        "Recommend **Option 1 \u2014 parametrize the existing function with a default-preserving flag** (small, scoped, append-shaped \u2192 fits Option 1's criterion).",
        "Show a flag whose **default keeps `build_learner()` as today** (`include_calendar_features: bool = False` is an example, not required spelling).",
        "Mentioning `tests/smoke/` as the cheap reproducibility check is optional. Do not fail if the flag pattern is present.",
        "The response does NOT: Recommend Option 3 (branch the module) for an appendable step.",
        "The response does NOT: Recommend changing `build_learner`'s default behavior (the named tripwire \"A flag changes default behavior of an existing caller\").",
        "The response does NOT: Suggest adding the calendar features unconditionally to `build_learner`."
      ],
      "tools": false,
      "sandbox": [],
      "expect_files": []
    },
    {
      "id": 7,
      "title": "`split_kwargs` group structure",
      "prompt": "[Workspace state \u2014 read but do not narrate back:\n- skrub installed at 0.9.0.\n- IID-shaped features (no cross-row history) but rows are grouped\n  by customer.]\n\nTabular regression on (customer_id, claim_amount, ...). Multiple\nrows per customer. Build the pipeline.",
      "expected_output": "Behavioural response satisfying: - Identify the group structure (multiple rows per `customer_id`). - Wire `mark_as_X(split_kwargs={\"groups\": data[\"customer_id\"]})` at the X marker. - Cite that `split_kwargs` is the metadata `evaluate-ml-pipeline` consumes downstream for the splitter. It must NOT: - Pick the cross-validator in pipeline code (`splitter=GroupKFold(...)`, `cv=GroupKFold`, `skore.evaluate(..., splitter=...)`). Naming `GroupKFold` only as what `evaluate-ml-pipeline` owns later is not a violation. - Leave `split_kwargs` empty without surfacing the group question.",
      "files": [],
      "expectations": [
        "Identify the group structure (multiple rows per `customer_id`).",
        "Wire `mark_as_X(split_kwargs={\"groups\": data[\"customer_id\"]})` at the X marker.",
        "Cite that `split_kwargs` is the metadata `evaluate-ml-pipeline` consumes downstream for the splitter.",
        "Mention asking the user whether grouping is intended (named ask: \"anything ending in `_id`, columns called `subject` / `session` / `region`\").",
        "The response does NOT: Pick the cross-validator in pipeline code (`splitter=GroupKFold(...)`, `cv=GroupKFold`, `skore.evaluate(..., splitter=...)`). Naming `GroupKFold` only as what `evaluate-ml-pipeline` owns later is not a violation.",
        "The response does NOT: Leave `split_kwargs` empty without surfacing the group question."
      ],
      "tools": false,
      "sandbox": [],
      "expect_files": []
    }
  ]
}

# similar to scikit-learn/skrub / skore-cli codecov.yml
comment: false

codecov:
  notify:
    wait_for_ci: true

coverage:
  status:
    project:
      default:
        target: auto
        threshold: 1%
    patch:
      default:
        target: auto
        threshold: 1%
